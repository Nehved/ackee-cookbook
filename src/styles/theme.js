export const colors = {
  blue: 'blue',
  // on real projects I keep all common colors there.
};

export const sizes = {
  mainWidth: '360px',
  mainHeight: '640px',
};

export default {
  colors,
  sizes,
};
